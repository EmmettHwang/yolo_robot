# ================================================================
# YOLOv5 + 휴머노이드 로봇 제어 통합 시스템 - requirements.txt
# ================================================================
#
# 설치 순서:
#   pip install -r requirements.txt
#   (yolov5 git clone 불필요 — ultralytics 패키지로 v5/v8/v11 모두 사용)
#
# Python 3.11 권장
# ================================================================

# --- 런타임 추론 (gradio 브랜치: OpenCV DNN + ONNX, torch 불필요) ---
opencv-python>=4.8.0
numpy>=1.23.0
pillow>=9.0.0

# --- 변환/학습 전용 (.pt → .onnx export, 학습 웹앱에서만 사용. 배포 exe엔 불필요) ---
ultralytics>=8.4.0      # export_onnx.py / 학습
torch>=2.0.0            #   "
torchvision>=0.15.0     #   "
onnx>=1.12.0            # ONNX export
gradio>=5.0             # 로컬 학습 웹앱(train_app.py)

# --- 로봇 시리얼 제어용 ---
pyserial>=3.5

# --- 오디오: 마이크 레벨/녹음·재생, 동작 사운드 ---
sounddevice>=0.4.6      # 마이크 VU / 녹음·재생, 스피커 테스트
pygame>=2.6.0           # 동작 사운드 mp3 재생
pyttsx3>=2.99           # 오프라인 TTS (Windows SAPI)
mutagen>=1.47           # mp3 메타정보(제목/아티스트/길이) 읽기
pymupdf>=1.27           # 매뉴얼 PDF 뷰어(렌더링/목차)
pywebview>=5.0          # 로고 클릭 시 웹뷰(kdt2025.com) 모달 (Windows: Edge WebView2)

# --- (선택) 음성 대화: 마이크→LLM→스피커 ---
SpeechRecognition>=3.10  # 음성 인식(Google Web Speech, 키 불필요)
# + 로컬 LLM은 Ollama 별도 설치 필요: https://ollama.com  (예: ollama pull llama3.2)
#   STT는 인터넷 필요, TTS는 pyttsx3(위) 사용

# --- 카메라 실제 이름 표시 (Windows, DirectShow) ---
pygrabber>=0.2          # 예: 'Logi C270 HD WebCam' (없으면 'Camera 0' 폴백)

# tkinter, configparser, threading, os, time 은 표준 라이브러리이므로 별도 설치 불필요.
# (Linux에서 tkinter가 없을 경우: sudo apt install python3-tk)
